

	<select class="texts" id="find_idUnit" name="find_idUnit" style="width:100%">
		<option value="">SILA PILIH</option>
		#foreach( $n in $listUnit)
			<option value="$n.id">$!n.noUnit.toUpperCase() $!n.namaUnit.toUpperCase()</option>
		#end
	</select>