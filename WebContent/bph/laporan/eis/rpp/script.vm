 function paparLaporan(idLaporan) {	
    	var div1 = document.getElementById('senaraiLaporan');
    	div1.style.display = 'none';	
    	var div2 = document.getElementById('paparLaporan');
    	div2.style.display = 'block'; 	
    	doDivAjaxCall3$formname('paparLaporan','paparLaporan','idLaporan='+ idLaporan); 
    }

function doCetakLaporanPembayaranRppBelumBayar() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
            var url = "../servlet/bph.laporan.eis.LaporanPembayaranRppBelumBayar?tahunSemasa="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanPembayaranRppSudahBayar() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
            var url = "../servlet/bph.laporan.eis.LaporanPembayaranRppSudahBayar?tahunSemasa="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanPembayaranRppBatal() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
            var url = "../servlet/bph.laporan.eis.LaporanPembayaranRppBatal?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanKehadiranRppHadir() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanKehadiranRppHadir?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanKehadiranRppTidakHadir() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanKehadiranRppTidakHadir?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanKehadiranRppBatal() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanKehadiranRppBatal?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanTempahanRppOnline() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanTempahanRppOnline?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanTempahanRppWalkin() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanTempahanRppWalkin?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanTempahanRppKelompok() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanTempahanRppKelompok?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}


function doCetakLaporanPenginapRppPenjawatAwam() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanPenginapRppPenjawatAwam?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanPenginapRppBadanBerkanun() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanPenginapRppBadanBerkanun?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}
function doCetakLaporanPenginapRppBadanBeruniform() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanPenginapRppBadanBeruniform?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}
function doCetakLaporanPenginapRppPesara() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanPenginapRppPesara?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanPenginapRppProfesional() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanPenginapRppProfesional?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanKadarPenginapanRppWaktuPuncak() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanKadarPenginapanRppWaktuPuncak?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanKadarPenginapanRppWaktuLuarPuncak() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanKadarPenginapanRppWaktuLuarPuncak?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}

function doCetakLaporanKutipanHasilRpp() {
	var err_count = 0;
	var tahun = $('tahun').value;
	if (err_count == 0){
              var url = "../servlet/bph.laporan.eis.LaporanKutipanHasilRpp?tahun="+tahun;
			var hWnd = window.open(url,'Cetak','full=yes, resizable=yes,scrollbars=yes');
			if ((document.window != null) && (!hWnd.opener))
			hWnd.opener = document.window;
			if (hWnd.focus != null) hWnd.focus();			
	}
	return err_count == 0;	
}