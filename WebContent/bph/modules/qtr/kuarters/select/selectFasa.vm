<select id="idFasa" name="idFasa" style="width:100%">
    <option value="">SILA PILIH</option>
    #foreach( $f in $selectFasa )
        <option #if ( $f.id ==  $!r.fasa.id) selected #end value="$f.id">$f.keterangan.toUpperCase()</option>
    #end
</select>
