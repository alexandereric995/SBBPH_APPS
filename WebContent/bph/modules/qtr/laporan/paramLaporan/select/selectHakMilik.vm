<select id="idHakMilik" name="idHakMilik" style="width:100%">
    <option value="">SILA PILIH</option>
    #foreach( $h in $selectHakMilik )
        <option value="$h.id">$!h.hakmilik.id.toUpperCase() - $!h.namaBlok.toUpperCase() - $!h.jenisStruktur.toUpperCase()</option>
    #end
</select>
