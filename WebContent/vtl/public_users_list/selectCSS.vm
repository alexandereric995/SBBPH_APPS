<select id="selectCSS" name="selectCSS" style="width:100%">
	 <option value="">SILA PILIH</option>
    #foreach( $css in $selectCSS )
    	<option value="$css.title" #if ($r.css.title == $css.title) selected #end >$css.title</option>
    #end
</select>