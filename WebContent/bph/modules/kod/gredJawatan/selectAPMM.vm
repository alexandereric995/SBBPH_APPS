<select id="idAPMM" name="idAPMM" style="width:100%" >
    <option value="">SILA PILIH</option>
    #foreach( $n in $selectAPMM)
    	<option #if ( $n.id == $r.apmm.id ) selected #end value="$n.id">$n.id.toUpperCase()-$n.keterangan.toUpperCase()</option>
    #end
</select>