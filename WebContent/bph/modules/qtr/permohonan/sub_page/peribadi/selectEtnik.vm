<select id="idEtnik" name="idEtnik" style="width:100%">
    <option value="">SILA PILIH</option>
    #foreach( $e in $selectEtnik )
        <option #if ( $e.id ==  $!users.etnik.id ) selected #end value="$e.id">$e.keterangan.toUpperCase()</option>
    #end
</select>
