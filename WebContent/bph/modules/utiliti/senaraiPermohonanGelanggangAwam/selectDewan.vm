<select id="idDewan" name="idDewan" style="width:100%" onChange="doDivAjaxCall3$formname('selectGelanggang','selectGelanggang','')">
	<option value="">SILA PILIH</option>
	#foreach( $n in $selectDewan)
		##<option #if ( $n.id ==  $!r.namaDewan.id) selected #end value="$n.id">$n.nama.toUpperCase()</option>
		<option #if ( $n.id ==  $!r.dewan.id) selected #end value="$n.id">$n.nama.toUpperCase()</option>
	
	#end
</select>


