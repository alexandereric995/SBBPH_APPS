<select id="idATM" name="idATM" style="width:100%" >
    <option value="">SILA PILIH</option>
    #foreach( $n in $selectATM )
    	<option #if ( $n.id == $r.atm.id ) selected #end value="$n.id">$n.id.toUpperCase()-$n.keterangan.toUpperCase()</option>
    #end
</select>